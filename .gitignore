
# Task runner output
.task/
output/

# Build artifacts (Task/Nuke)
bin/
obj/
build/_artifacts/*  # Versioned artifacts (R-BLD-070)
!build/_artifacts/.gitkeep

# Spec-kit - Protect AI agent credentials and tokens
# The .github/prompts/ directory contains slash command definitions (safe to commit)
# But .github/copilot/ may contain auth tokens (excluded below)
.github/copilot/
.specify/specs/*/secrets/

# Agent rules - Allow base rules and adapters, but exclude local overrides
!.agent/
!.agent/base/
!.agent/adapters/
!.agent/meta/
.agent/local/

# Spec-kit - Allow structure but protect sensitive data
!.specify/
!.specify/memory/
!.specify/scripts/
!.specify/templates/
